fare in modo che ogni volta che si esegue un action vengono registrate tutte le action e reaction che sono state eseguite
da quella azione a cascata e viene triggherato un nuovo update di stato. dopo di che nel updategamestate 
vengono riportate tutte le action e reaction che hanno portato a quel nuovo stato. il client quindi le metterà 
in una coda e ad ogni action collegherà un evento come delle animazioni che verranno eseguite in sequenza.


fare in modo che commandplayer possa essere usato tramite hook

king (summoning zone) una reaction before summoning che controlla se sta summonando nella zona giusta altrimeti aborta

fare path distance ecc per buchi nella grid se c'è un buco non farlo passare o costa piu energia

remove energy on summon/spawn
clean csbfg strings
tags

TESTING